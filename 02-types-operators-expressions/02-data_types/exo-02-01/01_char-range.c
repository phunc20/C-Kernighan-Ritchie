#include <stdio.h>
#include <math.h>

int main()
{
  char c = 0;
	int size = pow(2, 8);
	char start = c;
  printf("c = %d\n", c);
	//for (c=0; c<2*size; ++c)
  //  printf("c = %d\n", c);
	while 
	return 0;
}

